<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recrutement — Urgence Lilloise</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Montserrat', 'Segoe UI', sans-serif;
    }

    body {
      background-color: #0a0a0c;
      color: #ffffff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      background-image: linear-gradient(rgba(10, 10, 12, 0.7), rgba(10, 10, 12, 0.8)),
                        url('https://media.discordapp.net/attachments/1525200263173112018/1529295831117861006/wiwi.png?ex=6a655fbb&is=6a640e3b&hm=cf135396d2c321431e2cbf11e4793469e448ca210a13a972067362bd4e409c8e&=&format=webp&quality=lossless');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }

    /* HEADER */
    header {
      padding: 25px 60px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: rgba(10, 10, 12, 0.5);
      backdrop-filter: blur(8px);
    }

    .logo {
      font-size: 1.4rem;
      font-weight: 900;
      color: #ffffff;
      text-decoration: none;
      letter-spacing: 1px;
      text-transform: uppercase;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .logo img { height: 45px; width: auto; }
    .logo span { color: #e52d48; }

    nav { display: flex; gap: 30px; align-items: center; }
    nav a { color: #a0a0a0; text-decoration: none; font-weight: 600; font-size: 0.95rem; transition: color 0.3s; }
    nav a:hover, nav a.active { color: #ffffff; }

    /* CONTENU RECRUTEMENT */
    .container {
      max-width: 1000px;
      margin: 50px auto;
      padding: 0 20px;
      flex: 1;
    }

    .section-box {
      background: rgba(18, 19, 26, 0.85);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 16px;
      padding: 40px;
    }

    .header-box {
      display: flex;
      gap: 20px;
      align-items: flex-start;
      margin-bottom: 35px;
    }

    .icon-badge {
      background: rgba(229, 45, 72, 0.15);
      border: 1px solid rgba(229, 45, 72, 0.3);
      color: #e52d48;
      width: 60px;
      height: 60px;
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.6rem;
      flex-shrink: 0;
    }

    .header-info h1 {
      font-size: 1.9rem;
      font-weight: 800;
      margin-bottom: 8px;
    }

    .header-info p {
      color: #a0a0a0;
      font-size: 0.95rem;
      line-height: 1.5;
    }

    /* GRID DES POSTES */
    .jobs-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 25px;
    }

    .job-card {
      background: #14151f;
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 16px;
      padding: 30px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      transition: transform 0.3s, border-color 0.3s, box-shadow 0.3s;
    }

    .job-card:hover {
      transform: translateY(-5px);
      border-color: rgba(229, 45, 72, 0.5);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
    }

    .job-card-icon {
      background: rgba(229, 45, 72, 0.1);
      color: #e52d48;
      width: 50px;
      height: 50px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.3rem;
      margin-bottom: 20px;
    }

    .job-card h3 {
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 10px;
    }

    .job-card p {
      color: #8f92a1;
      font-size: 0.9rem;
      line-height: 1.5;
      margin-bottom: 25px;
    }

    .btn-apply {
      background: rgba(46, 213, 115, 0.1);
      color: #2ed573;
      border: 1px solid rgba(46, 213, 115, 0.3);
      padding: 12px 18px;
      border-radius: 10px;
      font-size: 0.9rem;
      font-weight: 700;
      cursor: pointer;
      text-align: center;
      transition: background 0.2s, color 0.2s;
      width: 100%;
    }

    .btn-apply:hover {
      background: #2ed573;
      color: #000000;
    }

    /* MODAL POPUP (FORMULAIRE) */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.85);
      backdrop-filter: blur(6px);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 1000;
      padding: 20px;
    }

    .modal-content {
      background: #14151f;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 16px;
      width: 100%;
      max-width: 650px;
      max-height: 90vh;
      overflow-y: auto;
      padding: 30px;
      position: relative;
    }

    .modal-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 25px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.08);
      padding-bottom: 15px;
    }

    .modal-header h2 {
      font-size: 1.4rem;
      font-weight: 800;
      color: #ffffff;
    }

    .close-btn {
      background: none;
      border: none;
      color: #a0a0a0;
      font-size: 1.5rem;
      cursor: pointer;
    }

    .form-section-title {
      font-size: 1rem;
      font-weight: 800;
      color: #e52d48;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin: 20px 0 12px 0;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .form-group {
      margin-bottom: 16px;
    }

    .form-group label {
      display: block;
      font-size: 0.85rem;
      font-weight: 600;
      margin-bottom: 6px;
      color: #cccccc;
    }

    .form-group input, .form-group textarea {
      width: 100%;
      background: #0a0a0c;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 8px;
      padding: 12px;
      color: #ffffff;
      font-size: 0.9rem;
      outline: none;
    }

    .form-group input:focus, .form-group textarea:focus {
      border-color: #e52d48;
    }

    .btn-submit {
      width: 100%;
      background: #e52d48;
      color: #ffffff;
      border: none;
      padding: 14px;
      border-radius: 8px;
      font-weight: 800;
      font-size: 0.95rem;
      cursor: pointer;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-top: 20px;
    }

    .btn-submit:hover {
      background: #c02138;
    }

    footer {
      background: #070708;
      border-top: 1px solid rgba(255, 255, 255, 0.05);
      padding: 20px;
      text-align: center;
      color: #666666;
      font-size: 0.85rem;
    }
    footer span { color: #e52d48; font-weight: 700; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header>
    <a href="/" class="logo">
      <img src="https://images-ext-1.discordapp.net/external/8aFZTPSGx86d0OrinnNccclsx-HM782VKGeEWOrsJd0/%3Fsize%3D256/https/cdn.discordapp.com/icons/1525134348842696825/2d0ad7849aa0c15ab794b22a214a4154.png?format=webp&quality=lossless" alt="Logo Urgence Lilloise">
      Urgence Lilloise <span>Semi FivePD</span>
    </a>
    <nav>
      <a href="/">Accueil</a>
      <a href="/recrutement" class="active">Recrutement</a>
      <a href="/boutique">Boutique</a>
    </nav>
  </header>

  <!-- CONTENU -->
  <div class="container">
    <div class="section-box">
      <div class="header-box">
        <div class="icon-badge"><i class="fa-solid fa-shield-halved"></i></div>
        <div class="header-info">
          <h1>Rejoindre l'équipe d'Urgence Lilloise</h1>
          <p>Nous recherchons des personnes motivées pour maintenir la qualité du Roleplay et faire rayonner le serveur. Postule dès maintenant ci-dessous !</p>
        </div>
      </div>

      <div class="jobs-grid">
        <!-- CARTE 1 : MODÉRATION -->
        <div class="job-card">
          <div>
            <div class="job-card-icon"><i class="fa-solid fa-user-shield"></i></div>
            <h3>Modération</h3>
            <p>Veille au bon déroulement du serveur en jeu et sur Discord. Assure le respect du règlement, gère la prise en charge des tickets et garantit une expérience RP fluide et de qualité pour tous les joueurs.</p>
          </div>
          <button class="btn-apply" onclick="openModal('Modération')">Recrutement ouvert — postuler</button>
        </div>

        <!-- CARTE 2 : COMMUNITY MANAGER -->
        <div class="job-card">
          <div>
            <div class="job-card-icon"><i class="fa-solid fa-headset"></i></div>
            <h3>Community Manager</h3>
            <p>Incarne et fais rayonner Urgence Lilloise ! Représente le serveur à travers des lives, la création de contenus (TikTok/YouTube) et l'animation active de nos réseaux sociaux et de la communauté.</p>
          </div>
          <button class="btn-apply" onclick="openModal('Community Manager')">Recrutement ouvert — postuler</button>
        </div>
      </div>
    </div>
  </div>

  <!-- MODAL FORMULAIRE POPUP -->
  <div class="modal-overlay" id="modalOverlay">
    <div class="modal-content">
      <div class="modal-header">
        <h2 id="modalTitle">Postuler</h2>
        <button class="close-btn" onclick="closeModal()">&times;</button>
      </div>
      <form id="applyForm">
        <input type="hidden" id="roleInput" name="service">

        <!-- SECTION 1 : DISCORD -->
        <div class="form-section-title"><i class="fa-brands fa-discord"></i> Informations : Discord</div>
        
        <div class="form-group">
          <label>Pseudo Discord :</label>
          <input type="text" name="discordTag" required placeholder="Ex: ymn_0ffcl">
        </div>

        <div class="form-group">
          <label>Identifiant Discord (ID Utilisateur) :</label>
          <input type="text" name="discordId" required placeholder="Ex: 1525200263173112018">
        </div>

        <!-- SECTION 2 : IRL -->
        <div class="form-section-title"><i class="fa-solid fa-user"></i> Informations : IRL</div>

        <div class="form-group">
          <label>Prénom :</label>
          <input type="text" name="prenom" required placeholder="Votre prénom...">
        </div>

        <div class="form-group">
          <label>Âge :</label>
          <input type="number" name="age" required placeholder="Ex: 18">
        </div>

        <div class="form-group">
          <label>Ambition :</label>
          <textarea name="ambition" rows="2" required placeholder="Quelles sont vos ambitions au sein de la communauté ?"></textarea>
        </div>

        <!-- SECTION 3 : MOTIVATION -->
        <div class="form-section-title"><i class="fa-solid fa-fire"></i> Informations : Motivation</div>

        <div class="form-group">
          <label>Pourquoi vous et pas un autre ?</label>
          <textarea name="motivation" rows="3" required placeholder="Exposez vos motivations et vos atouts..."></textarea>
        </div>

        <div class="form-group">
          <label>Vos expériences (si vous en avez, citez-les) :</label>
          <textarea name="experience" rows="3" placeholder="Vos expériences passées en modération / administration..."></textarea>
        </div>

        <div class="form-group">
          <label>Dans vos mots, en quoi consiste le rôle d'un modérateur ?</label>
          <textarea name="roleModerateur" rows="3" required placeholder="Expliquez votre vision du rôle de modérateur..."></textarea>
        </div>

        <button type="submit" class="btn-submit">Envoyer ma candidature</button>
      </form>
    </div>
  </div>

  <footer>
    <p>&copy; 2026 Urgence Lilloise. Tous droits réservés. — Made by <span>ymn_0ffcl</span></p>
  </footer>

<script>
    function openModal(roleName) {
      document.getElementById('modalTitle').innerText = "Postuler au poste de " + roleName;
      document.getElementById('roleInput').value = roleName;
      document.getElementById('modalOverlay').style.display = 'flex';
    }

    function closeModal() {
      document.getElementById('modalOverlay').style.display = 'none';
    }

    document.getElementById('applyForm').addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const formData = new FormData(this);
      const data = Object.fromEntries(formData.entries());

      try {
        const response = await fetch('/recrutement', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });

        if (response.ok) {
          alert("✅ Candidature envoyée avec succès au staff !");
          closeModal();
          this.reset();
        } else {
          const errData = await response.json();
          alert("❌ " + (errData.error || "Erreur lors de l'envoi de la candidature."));
        }
      } catch (err) {
        alert("❌ Impossible de contacter le serveur.");
      }
    });
  </script>
</body>
</html>
